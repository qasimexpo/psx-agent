# Pipeline dependencies (GitHub Actions only - nothing here is on a page request path)
requests>=2.31.0
beautifulsoup4>=4.12.0
feedparser>=6.0.11
python-dotenv>=1.0.0
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.9
groq>=0.9.0
# Gemini is called over plain REST (see pipeline/llm.py). The official SDK is
# deprecated and rejects the newer AQ. style keys AI Studio now issues.
# ZoneInfo needs an explicit tz database on Windows and on slim containers.
tzdata>=2024.1
